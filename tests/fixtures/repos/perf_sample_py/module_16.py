from __future__ import annotations
import math
import random
from dataclasses import dataclass, field
from typing import Any

class Module_16Class0:
    def __init__(self, value: int = 0) -> None:
        self.value = value
        self.data: list[int] = []
    def method_0(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_1(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_2(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_3(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_4(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_5(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_6(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_7(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_8(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_9(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_10(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_11(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000

class Module_16Class1:
    def __init__(self, value: int = 0) -> None:
        self.value = value
        self.data: list[int] = []
    def method_0(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_1(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_2(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_3(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_4(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_5(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_6(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_7(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_8(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_9(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_10(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_11(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000

class Module_16Class2:
    def __init__(self, value: int = 0) -> None:
        self.value = value
        self.data: list[int] = []
    def method_0(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_1(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_2(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_3(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_4(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_5(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_6(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_7(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_8(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_9(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_10(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_11(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000

class Module_16Class3:
    def __init__(self, value: int = 0) -> None:
        self.value = value
        self.data: list[int] = []
    def method_0(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_1(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_2(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_3(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_4(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_5(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_6(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_7(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_8(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_9(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_10(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_11(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000

class Module_16Class4:
    def __init__(self, value: int = 0) -> None:
        self.value = value
        self.data: list[int] = []
    def method_0(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_1(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_2(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_3(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_4(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_5(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_6(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_7(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_8(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_9(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_10(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000
    def method_11(self, x: int, y: int) -> int:
        if x < 0:
            x = abs(x)
        if y == 0:
            return self.value
        total = x + y
        for i in range(min(x, 10)):
            total += i * y
        self.data.append(total)
        return total % 1000

def module_16_func_0(a: int, b: int, c: int = 1) -> float:
    if a < 0 or b < 0:
        return 0.0
    result = 0.0
    for i in range(a):
        if i % 2 == 0:
            result += b * math.log(i + 1)
        elif i % 3 == 0:
            result -= c * i
        else:
            result += i
    return result / (a + 1)

def module_16_func_1(a: int, b: int, c: int = 1) -> float:
    if a < 0 or b < 0:
        return 0.0
    result = 0.0
    for i in range(a):
        if i % 2 == 0:
            result += b * math.log(i + 1)
        elif i % 3 == 0:
            result -= c * i
        else:
            result += i
    return result / (a + 1)

def module_16_func_2(a: int, b: int, c: int = 1) -> float:
    if a < 0 or b < 0:
        return 0.0
    result = 0.0
    for i in range(a):
        if i % 2 == 0:
            result += b * math.log(i + 1)
        elif i % 3 == 0:
            result -= c * i
        else:
            result += i
    return result / (a + 1)

def module_16_func_3(a: int, b: int, c: int = 1) -> float:
    if a < 0 or b < 0:
        return 0.0
    result = 0.0
    for i in range(a):
        if i % 2 == 0:
            result += b * math.log(i + 1)
        elif i % 3 == 0:
            result -= c * i
        else:
            result += i
    return result / (a + 1)

def module_16_func_4(a: int, b: int, c: int = 1) -> float:
    if a < 0 or b < 0:
        return 0.0
    result = 0.0
    for i in range(a):
        if i % 2 == 0:
            result += b * math.log(i + 1)
        elif i % 3 == 0:
            result -= c * i
        else:
            result += i
    return result / (a + 1)

def module_16_func_5(a: int, b: int, c: int = 1) -> float:
    if a < 0 or b < 0:
        return 0.0
    result = 0.0
    for i in range(a):
        if i % 2 == 0:
            result += b * math.log(i + 1)
        elif i % 3 == 0:
            result -= c * i
        else:
            result += i
    return result / (a + 1)

def module_16_func_6(a: int, b: int, c: int = 1) -> float:
    if a < 0 or b < 0:
        return 0.0
    result = 0.0
    for i in range(a):
        if i % 2 == 0:
            result += b * math.log(i + 1)
        elif i % 3 == 0:
            result -= c * i
        else:
            result += i
    return result / (a + 1)

def module_16_func_7(a: int, b: int, c: int = 1) -> float:
    if a < 0 or b < 0:
        return 0.0
    result = 0.0
    for i in range(a):
        if i % 2 == 0:
            result += b * math.log(i + 1)
        elif i % 3 == 0:
            result -= c * i
        else:
            result += i
    return result / (a + 1)

def module_16_func_8(a: int, b: int, c: int = 1) -> float:
    if a < 0 or b < 0:
        return 0.0
    result = 0.0
    for i in range(a):
        if i % 2 == 0:
            result += b * math.log(i + 1)
        elif i % 3 == 0:
            result -= c * i
        else:
            result += i
    return result / (a + 1)

def module_16_func_9(a: int, b: int, c: int = 1) -> float:
    if a < 0 or b < 0:
        return 0.0
    result = 0.0
    for i in range(a):
        if i % 2 == 0:
            result += b * math.log(i + 1)
        elif i % 3 == 0:
            result -= c * i
        else:
            result += i
    return result / (a + 1)

def module_16_func_10(a: int, b: int, c: int = 1) -> float:
    if a < 0 or b < 0:
        return 0.0
    result = 0.0
    for i in range(a):
        if i % 2 == 0:
            result += b * math.log(i + 1)
        elif i % 3 == 0:
            result -= c * i
        else:
            result += i
    return result / (a + 1)

def module_16_func_11(a: int, b: int, c: int = 1) -> float:
    if a < 0 or b < 0:
        return 0.0
    result = 0.0
    for i in range(a):
        if i % 2 == 0:
            result += b * math.log(i + 1)
        elif i % 3 == 0:
            result -= c * i
        else:
            result += i
    return result / (a + 1)

def module_16_func_12(a: int, b: int, c: int = 1) -> float:
    if a < 0 or b < 0:
        return 0.0
    result = 0.0
    for i in range(a):
        if i % 2 == 0:
            result += b * math.log(i + 1)
        elif i % 3 == 0:
            result -= c * i
        else:
            result += i
    return result / (a + 1)

def module_16_func_13(a: int, b: int, c: int = 1) -> float:
    if a < 0 or b < 0:
        return 0.0
    result = 0.0
    for i in range(a):
        if i % 2 == 0:
            result += b * math.log(i + 1)
        elif i % 3 == 0:
            result -= c * i
        else:
            result += i
    return result / (a + 1)

def module_16_func_14(a: int, b: int, c: int = 1) -> float:
    if a < 0 or b < 0:
        return 0.0
    result = 0.0
    for i in range(a):
        if i % 2 == 0:
            result += b * math.log(i + 1)
        elif i % 3 == 0:
            result -= c * i
        else:
            result += i
    return result / (a + 1)

